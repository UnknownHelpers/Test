import React from "react";
import sinon from "sinon";
import {shallow} from "enzyme";
import _ from "lodash";
import SimpleTable from "../Business-Administration/Rule-Administration/index";

describe("SimpleTable", () => {
    let _wrapper, _state;

    it("Simple test", () => {
        const _val = 1;
        expect(_val).toEqual(1);
    }); 
})